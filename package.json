{
  "name": "typescript-repo",
  "version": "1.0.0",
  "description": "```bash\r ## install globally typescript ##\r npm install -g typescript\r ## gives elevated privileges to run commands ##\r sudo\r ## create a project ##\r npm init -y\r ## add typescript as a dev dependency ##\r npm install typescript --save-dev\r ## create typescript configuration file ##\r npx tsc --init\r ## compile typescript ##\r tsc\r ## run page of code ##\r node filename\r ## run \"scripts\" command from package.json (\"test\": \"tsc && node test.js\")##\r npm run dev\r ## create typescript project with vite##\r npm init vite@latest projectName -- --template vanilla-ts\r ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
